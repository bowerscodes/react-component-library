import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import ErrorMessage from '.';
import BackToTop from '../BackToTop';

<Meta title='ErrorMessage' component={ErrorMessage} />

# Error Message

The `<ErrorMessage>` component is used to display an error message to the user.

<Canvas>
  <Story name='Default'>
    <ErrorMessage children='This is an Error message.' />
  </Story>
</Canvas>

### Props
<ArgsTable of={ErrorMessage}/>

<BackToTop/>
