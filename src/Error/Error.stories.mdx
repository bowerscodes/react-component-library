import { Canvas, Meta, Story } from '@storybook/addon-docs';
import Error from './';

<Meta title='Error' component={Error} />

# Error

The `<Error>` component is used to display an error message to the user.

<Canvas>
  <Story name='Default'>
    <Error children='This is an Error message.' />
  </Story>
</Canvas>
